# v3 reproducibility notes

Recommended metadata to record for every serious v3 run:

- input corpus file names and checksums;
- number of unique peptides after filtering;
- model hyperparameters;
- random seed;
- training loss history;
- number of generated raw candidates;
- number passing filters;
- top-panel selection criteria;
- external predictor versions used for post-hoc scoring.
