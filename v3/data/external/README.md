# v3 external comparator data

This folder stores small external comparator tables.

Included:

```text
apex_oracle_ranked_summary.csv
```

This APEX/ApexOracle table is used only as a computational comparator. It is not treated as wet-lab ground truth.
