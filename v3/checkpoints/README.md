# v3 checkpoints

Trained AMP-JEPA-Hybrid v3 checkpoints are written here.

Expected main output:

```text
v3/checkpoints/amp_jepa_hybrid_v3.pt
```

Checkpoint files are model artifacts and should usually not be committed to Git unless they are intentionally small release artifacts.
